{{!-- grid based card system that will be 3 wide with space between of the artworks --}}

<section class="container">
  {{!-- gallery will be replaced when we build out db --}}
    {{#each gallery.artwork as |artCard|}}
      {{> artwork-details}}
    {{/each}}
</section>
